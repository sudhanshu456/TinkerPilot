name: Build and Release

on:
  release:
    types: [created]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies and build frontend
        run: |
          cd frontend
          npm install
          npm run build
        
      - name: Build Python Wheel and Package Release
        run: |
          # Copy compiled frontend into backend module for the pip package
          mkdir -p backend/app/static/ui
          cp -r frontend/out/* backend/app/static/ui/
          
          # Remove massive node_modules to keep everything clean
          rm -rf frontend/node_modules
          rm -rf .git .github
          
          # Create the source tarball containing the whole repo (used by install.sh)
          tar --exclude=tinkerpilot-release.tar.gz -czf tinkerpilot-release.tar.gz -C . .

          # Build the .whl using standard python tools
          cd backend
          pip install build
          python -m build --wheel
          mv dist/*.whl ../tinkerpilot-backend-any.whl
          
      - name: Upload Release Tarball
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ github.event.release.upload_url }}
          asset_path: ./tinkerpilot-release.tar.gz
          asset_name: tinkerpilot-release.tar.gz
          asset_content_type: application/gzip

      - name: Upload Python Wheel
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ github.event.release.upload_url }}
          asset_path: ./tinkerpilot-backend-any.whl
          asset_name: tinkerpilot-backend-any.whl
          asset_content_type: application/zip
